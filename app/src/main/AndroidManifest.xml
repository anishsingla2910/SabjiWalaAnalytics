<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_my_sabji_wala_icon"
        android:label="@string/app_name"
        android:roundIcon="@drawable/ic_my_sabji_wala_icon"
        android:supportsRtl="true"
        android:theme="@style/Theme.SabjiWalaAnalytics.NoActionBar"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules">
        <activity android:name=".add_category.AddCategoryActivity"/>
        <activity android:name=".add_vegetable.AddVegetableActivity" />
        <activity android:name=".MainActivity" />
        <activity android:name=".login.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".login.VerifyPhoneActivity" />
    </application>

</manifest>